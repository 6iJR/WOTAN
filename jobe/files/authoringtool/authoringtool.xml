<?xml version="1.0"?>
<root>
    <htmlfile>
        <filename></filename> <!-- without .html -->
        <pagetitle></pagetitle>
        <introtext></introtext>
        <code>public int intMax(int a, int b, int c) {
  int max;
  
  // First check between a and b
  if (a > b) {
    max = a;
  } else {
    max = b;
  }
  
  // Now check between max and c
  if (c > max) {
    max = c;
  }
  
  return max;
  
  // Could use the built in Math.max(x, y) function which selects the larger
  // of two values.
}</code> <!-- the starting code. Watch format! -->
    </htmlfile>
    <testfile methodname=""> <!-- repeat just the method name from <code>. Example: intMax -->
        <test>
            <message></message> <!-- Example: intMax(1, 2, 3) -> 3 -->
            <expectedvalue></expectedvalue> <!-- Examples: 3, 3.14, "foo" -->
            <parameter></parameter>
            <hidden></hidden> <!-- 0 or 1 -->
            <hint></hint> <!-- -->
        </test>
    </testfile>
</root>